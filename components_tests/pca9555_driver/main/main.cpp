#include <esp_log.h>
#include <inttypes.h>
#include <stdio.h>

static const char *TAG = "Test PCA9555";
constexpr uint8_t SDA_PIN = 10;
constexpr uint8_t SCL_PIN = 11;

extern "C" {
void app_main(void) {
        while (1) {
        }
}
}
